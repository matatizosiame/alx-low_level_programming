#include "main.h"
/**
  *_isupper - check if the letter is upper
  *@c: the number to be checked
  *Return: 1 for upper and 0 else
  */
int _isupper(int c)
{
	if (c >= 65; &&c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
